--- a/net/minecraft/world/entity/player/Player.java
+++ b/net/minecraft/world/entity/player/Player.java
@@ -12,6 +_,8 @@
 import java.util.Optional;
 import java.util.OptionalInt;
 import java.util.function.Predicate;
+
+import de.erethon.papyrus.CraftPDamageType;
 import net.minecraft.core.BlockPos;
 import net.minecraft.core.Direction;
 import net.minecraft.core.GlobalPos;
@@ -711,7 +_,7 @@
     }
 
     @Override
-    public boolean hurtServer(final ServerLevel level, final DamageSource source, float damage) {
+    public boolean hurtServer(final ServerLevel level, final DamageSource source, float damage, CraftPDamageType damageType) {
         if (this.isInvulnerableTo(level, source)) {
             return false;
         } else if (this.abilities.invulnerable && !source.is(DamageTypeTags.BYPASSES_INVULNERABILITY)) {
@@ -738,7 +_,7 @@
 
                 // return damage != 0.0F && super.hurtServer(level, source, damage);
                 // CraftBukkit start - Don't filter out 0 damage
-                boolean damaged = super.hurtServer(level, source, damage);
+                boolean damaged = super.hurtServer(level, source, damage, damageType);
                 if (damaged) {
                     this.removeEntitiesOnShoulder();
                 }
@@ -1020,7 +_,7 @@
 
         if (playerAttackEntityEvent.callEvent() && willAttack) { // Logic moved to willAttack local variable.
         // Paper end - PlayerAttackEntityEvent
-            float baseDamage = this.isAutoSpinAttack() ? this.autoSpinAttackDmg : (float)this.getAttributeValue(Attributes.ATTACK_DAMAGE);
+            float baseDamage = this.isAutoSpinAttack() ? this.autoSpinAttackDmg : (float)this.getAttributeValue(attackDamageType.advantage); // Papyrus
             ItemStack attackingItemStack = this.getWeaponItem();
             DamageSource damageSource = this.createAttackSource(attackingItemStack); final DamageSource dmgSourceFinal = damageSource; // Paper - damage events
             float attackStrengthScale = this.getAttackStrengthScale(0.5F);
@@ -1055,7 +_,15 @@
                     }
 
                     Vec3 oldMovement = entity.getDeltaMovement();
-                    boolean wasHurt = entity.hurtOrSimulate(damageSource, totalDamage);
+                    // Papyrus start - Add attributes
+                    boolean wasHurt;
+                    if (entity instanceof LivingEntity livingEntity) {
+                        totalDamage = (float) getBukkitEntity().onAttack(livingEntity.getBukkitLivingEntity(), totalDamage, attackDamageType.apiType);
+                        wasHurt = livingEntity.hurtServer((ServerLevel) level(), damageSource, totalDamage, attackDamageType);
+                    } else {
+                        wasHurt = entity.hurtOrSimulate(damageSource, totalDamage);
+                    }
+                    // Papyrus end
                     if (wasHurt) {
                         this.causeExtraKnockback(entity, this.getKnockback(entity, damageSource) + (knockbackAttack ? 0.5F : 0.0F), oldMovement);
                         if (sweepAttack) {
@@ -1250,7 +_,7 @@
                     float enchantedDamage = this.getEnchantedDamage(nearby, var12, damageSource) * attackStrengthScale;
                     // Paper start - Only apply knockback if the event is not canceled
                     nearby.lastDamageCancelled = false;
-                    if (nearby.hurtServer(serverLevel, damageSource.knownCause(org.bukkit.event.entity.EntityDamageEvent.DamageCause.ENTITY_SWEEP_ATTACK), enchantedDamage) && !nearby.lastDamageCancelled) {
+                    if (nearby.hurtServer(serverLevel, damageSource.knownCause(org.bukkit.event.entity.EntityDamageEvent.DamageCause.ENTITY_SWEEP_ATTACK), enchantedDamage, attackDamageType) && !nearby.lastDamageCancelled) {
                         // Paper end - Only apply knockback if the event is not canceled
                         nearby.knockback(0.4F, Mth.sin(this.getYRot() * Mth.DEG_TO_RAD), -Mth.cos(this.getYRot() * Mth.DEG_TO_RAD), this, io.papermc.paper.event.entity.EntityKnockbackEvent.Cause.SWEEP_ATTACK); // Paper - knockback events
                         EnchantmentHelper.doPostAttackEffects(serverLevel, nearby, damageSource);
